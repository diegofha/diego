#include <iostream>
#include <conio.h>
using namespace std;
void main(){
	int n,r=0;
		cout<<"introducir numero";
		cin>>n;

	    while(n>0)
		{
			n=n / 10;
			r++;
		}
		cout<<"los digitos del numero son:"<<r;
		getch();
}
